{
  "name": "claude-plugins-dev",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling for the claude-plugins marketplace (drift-guard tests). Not required to use any plugin.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
